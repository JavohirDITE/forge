# Copy to .env. `docker compose up -d` provides a matching Postgres on 5433.
DATABASE_URL="postgresql://forge:forge@localhost:5433/forge?schema=public"

# 32+ char random string. Generate: `openssl rand -base64 32`
AUTH_SECRET="change-me"
